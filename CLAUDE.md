# Team Skills (Claude Code)

This is a skill catalog, not an application. Follow `AGENTS.md`.

Canonical skills: `skills/<audience>/<skill-name>/SKILL.md`.

When adding or editing a skill, read `skills/shared/team-skill-authoring/SKILL.md` first, then run `python3 scripts/validate.py`.
